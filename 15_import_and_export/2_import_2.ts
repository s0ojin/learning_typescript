import { IdolModel, rose, number, ICat } from "./2_export_1"; // 이름을 지어줄 수 없음

const iu = new IdolModel("아이유", 30);
console.log(iu);

console.log(rose);
console.log(number);

const cat: ICat = {
  name: "냥냥",
  breed: "러시안블루",
};

console.log(cat);
